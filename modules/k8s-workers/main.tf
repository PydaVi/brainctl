# TODO: módulo k8s-workers v2. Este arquivo existe para definir o entrypoint do módulo.
# Ele é gerado como stub para preservar a estrutura de módulos sob Terragrunt.

terraform {
  # Necessário para que o remote_state do Terragrunt tenha efeito.
  backend "s3" {}
}
